package Workflow::Dispatcher::Sge;

use strict;
use warnings;

class Workflow::Dispatcher::Sge {
    is => 'Workflow::Dispatcher',
};

sub execute {
    my $self = shift;
    my $job = shift;
}

sub get_command {
}
